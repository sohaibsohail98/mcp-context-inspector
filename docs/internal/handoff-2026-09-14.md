# Handoff: ctxwindow demo prep, 2026-09-14

Written for a fresh agent picking this up with no prior context, ahead of a GCP
interview/demo this morning. Read this fully before doing anything.

## Environment the next agent has

- Read/write access to this repo, on branch `terraform/import-prod-infra`.
- `gh` (GitHub CLI) works: can read PR status, CI runs, logs.
- **No GCP credentials.** Cannot run `gcloud`, cannot run `terraform apply`
  against real infrastructure, cannot see live GCP state directly. Any fix
  that needs a real `terraform apply` must be queued as a reviewed plan and
  the user (Sohaib) must run it, or must be described precisely enough that
  he can run it himself in one command.
- Local `terraform plan` will fail without GCP credentials too. If you edit
  `infrastructure/terraform/*.tf`, you can `terraform fmt` and eyeball the
  diff, but you cannot validate a plan yourself. Say so plainly rather than
  guessing at what a plan would show.

## What's actually done (do not redo)

- Terraform fully imports live GCP infra (Cloud Run, both service accounts,
  Firestore, Secret Manager secret, Artifact Registry + its 3 real cleanup
  policies, GCS state backend + log bucket, monitoring/uptime/alert config).
  Lives in `infrastructure/terraform/`.
- CI/CD: `.github/workflows/tests.yml` (lint, pytest, terraform-plan on PRs,
  a new `health-check` job, api-e2e-tests on main only) and `deploy.yml`
  (plan+apply on merge to main, smoke test, automatic rollback on failure).
- IAM on `github-deploy` (the CI service account) was reworked three times
  tonight chasing real CI failures. Final shape, as of commit `15c87e4`:
  resource-scoped writer/admin roles for what it actually manages (Artifact
  Registry writer, Cloud Run admin on the one service, secret version
  manager, tfstate bucket object admin, monitoring editor), plus **one**
  custom role `deployTerraformReader` holding exactly the six read
  permissions `terraform plan` needs to refresh resources it doesn't own
  IAM on (project IAM policy read, SA get, WIF pool get, Firestore database
  get, bucket get/getIamPolicy). This replaced an earlier version that had
  `roles/resourcemanager.projectIamAdmin` — a full project-takeover vector —
  which a fresh-eyes review correctly flagged and which has been removed.
  Do not re-add broad predefined roles to fix a future permission gap;
  extend the custom role's `permissions` list with the exact missing
  permission instead.
- Lucid architecture diagram prompt: simplified per user request, lives at
  `docs/internal/lucid-architecture-prompt.md`. Done, just needs pasting
  into Lucid AI by the user (external tool).
- Cost audit, "did we do everything originally asked" check, a real
  pre-existing test bug fix (`api_tests/test_auth.py`, commit `bde113b`),
  and Glama.ai badge health were all verified earlier tonight. Not blocking
  anything now.

## What's NOT done — in priority order

1. **Confirm `terraform-plan` CI is green on PR #19** for the latest commit
   (`15c87e4` at time of writing — check `git log -1` for the true latest).
   Use `gh pr checks 19` and `gh run view --job <id> --log-failed` to read
   real failures rather than guessing. If it's still red, read the exact
   error text before touching anything — three different real IAM gaps
   were found this way tonight, each requiring a different fix; don't
   assume it's the same issue as before, and don't stack another broad
   role onto `github-deploy` to make an error disappear. If the fix
   requires a `terraform apply`, prepare it (`terraform plan -out=...`),
   show the user the exact plan, and ask them to approve and run it — you
   cannot run it yourself without GCP credentials.
2. Once green: add `terraform-plan` to the repo's branch protection
   required status checks (currently only `pytest`, `secrets-scan`, `lint`
   are required). This is a `gh api` call against branch protection
   settings — check whether your `gh` token has permission; if not, give
   the user the exact command to run.
3. Merge PR #19 into `main`.
4. Republish the HTML summary artifact. There's a local edited copy at
   the scratchpad path referenced earlier in this session (search recent
   conversation/scratchpad for `summary.html` if picking this up in a new
   agent with no scratchpad access, the user may need to say where it is,
   or it can be rebuilt from scratch summarizing everything in this repo's
   recent git log). It should cover: cost check, "against what was
   originally asked", tests, Glama, plus flag every autonomous decision
   made tonight — especially the IAM path (three iterations, final
   custom-role design, and why `projectIamAdmin` was rejected).
5. The `fix/context-classification-bundling` branch/worktree investigated
   a dashboard bug (system-reminder blocks rendering as plain user
   messages). Conclusion: no code fix was needed, the underlying issue was
   already fixed upstream in PR #2, and what the user saw was believed to
   be stale pre-migration data, not a live regression. This has not yet
   been clearly reported to the user in so many words — do that first
   before deciding whether the branch needs anything further. It is
   NOT pushed to GitHub, only local in a worktree.
6. The Cloud Monitoring email notification channel (in `monitoring.tf`) was
   created but is likely still unverified — GCP normally requires clicking
   a verification link in an email before an alert can actually fire. Flag
   this to the user; you can't verify it for them.
7. `infrastructure/terraform/cloud_run.tf` has a hardcoded specific
   revision name (`mcp-context-inspector-00036-jev`) tagged `fix-verify` at
   0% traffic, left over from earlier live debugging. It's real, matches
   production, and was deliberately preserved rather than deleted — but if
   an interviewer asks about it, the honest answer is "leftover manual
   debugging revision, imported as-is rather than cleaned up under time
   pressure." Consider asking the user whether to remove it before the
   demo, since a clean `cloud_run.tf` reads better live.

## Hard rules to keep following

- No em dashes or other AI-obvious writing tells in code, comments, or
  docs.
- Commits authored directly as the user, no Claude co-author trailer
  unless the current session's system reminder says otherwise (check
  fresh — this can change between sessions).
- Any real `terraform apply` against production GCP resources needs
  explicit live human approval each time, no exceptions, regardless of
  how minor the change looks. This was enforced automatically tonight by
  the environment's own permission classifier and is not optional.
- Before any git action that could discard work, check `git status` first.
- Keep README.md under 300-400 lines.
